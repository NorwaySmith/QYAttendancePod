//
//  QYTheme.h
//  QYBaseProject
//
//  Created by 田 on 15/6/4.
//  Copyright (c) 2015年 田. All rights reserved.
//

#import <UIKit/UIKit.h>

/**
 *  一行列表高度
 */
static CGFloat const ThemeListHeightSingle = 48.0f;
/**
 *  二行列表高度
 */
static CGFloat const ThemeListHeightDouble = 68.0f;
/**
 *  二行列表图片尺寸
 */
static CGFloat const ThemeListPicSizeDouble = 48.0f;
/**
 *  一行列表高度(新)
 */
static CGFloat const ThemeListHeightSingleNew = 42.0f;
/**
 *  二行列表高度(新)
 */
static CGFloat const ThemeListHeightDoubleNew = 56.0f;
/**
 *  二行列表图片尺寸(新)
 */
static CGFloat const ThemeListPicSizeDoubleNew = 40.0f;
/**
 *  二行cell图片距离顶部距离
 */
static CGFloat const ThemeListPicDistanceTop = 8.0f;
/**
 *  三行列表高度
 */
static CGFloat const ThemeListHeightThird = 80.0f;

/**
 *  三行列表图片尺寸
 */
static CGFloat const ThemeListPicSizeThird = 64.0f;


/**
 *  三行列表图片尺寸 IM列表高度
 */
static CGFloat const ThemeIMListHeightThird = 135.0f/2;


/**
 *  列表右箭头距离右边距的距离，参考系统自己的右箭头
 */
static CGFloat const ThemeListRightArrowImageToRight = 16.0f;

/**
 *  nav的左侧返回按钮距离左边距的激励，参考系统自己的返回按钮
 */
static CGFloat const ThemeLeftBackgroundToLeftSide = 9.0f;

/**
 *  IM列表头像高度
 */
static CGFloat const ThemeIMHeaderHeight = 100.0f/2;

/**
 *  IM列表头像 距离顶部的距离
 */
static CGFloat const ThemeIMHeaderTopSpace = 18.0f/2;

/**
 *  IM列表头像 距离左侧的距离
 */
static CGFloat const ThemeIMHeaderLeftSpace = 20.0f/2;


/**
 *  IM列表时间 距离顶部的距离
 */
static CGFloat const ThemeIMTimeTopSpace = 33.0f/2;
/**
 *  IM列表时间 距离右侧的距离
 */
static CGFloat const ThemeIMTimeRightSpace = 22.0f/2;


/**
 *  发现 头像 cell 高度
 */
static CGFloat const ThemeListCellHeight = 90.0f;

/**
 *  发现 头像 size
 */
static CGFloat const ThemeListCellInHeadSize = 60.0f;

/**
 *  发现 默认cell headImage Size
 */
static CGFloat const ThemeListCellHeadViewSize = 23.0f;


//间距
static CGFloat const padding = 12;
//左右间距 设定为12
static CGFloat const levelPadding       = 12;


//例如：发送、发起 button
//上间距
static CGFloat const buttonTopPadding = 20;

//左右间距
static CGFloat const buttonLeftOrRightPadding = 10;


#pragma mark - 选人控件样式

/**
 *  选人控件单元格高度
 */
static CGFloat const ThemePickerCellRowHeight = 44.0f;

/**
 *  选人控件标题高度
 */
static CGFloat const ThemePickerHeaderRowHeight = 30.0f;

/**
 *  选人控件标题高度
 */
static CGFloat const ThemePickerHeaderRowHeightStyle1 = 26.0f;

/**
 *  选人控件名称高度
 */
static CGFloat const ThemePickerTitleHeight = 16.0f;

/**
 *  选人控件部门高度
 */
static CGFloat const ThemePickerDepartmentHeight = 14.0f;

/**
 *  选人控件名称高度
 */
static CGFloat const ThemePickerTitleHeightStyle1 = 12.0f;

/**
 *  选人控件头像名称间隔
 */
static CGFloat const ThemePickerIconAndNamePadding = 3.0f;

/**
 *  选人控件头像大小
 */
static CGFloat const ThemePickerIconSize = 32.0f;

/**
 *  选人控件选择按钮大小
 */
static CGFloat const ThemePickerCheckSize = 22.0f;

/**
 *  选择人员工具条高度
 */
static CGFloat const ThemeToolbarHeight = 55.0f;

/**
 *  选择人员工具条中头像大小
 */
static CGFloat const ThemeToolbarItemSize = 42.0f;

/**
 *  选择人员工具条中头像大小
 */
static CGFloat const ThemeNameAndDepartmentPadding = 2.0f;

// 表格分割线
static CGFloat const sepratorLineHeight = 0.5;



//通讯录规范
/**
 *  通讯录cell高度
 */
static CGFloat const ABCellHEIGHT = 55.0f;

/**
 *  通讯录头像大小
 */
static CGFloat const ABHeaderSIZE = 36;



#ifndef QYBaseProject_QYTheme_h
#define QYBaseProject_QYTheme_h


#import "UIColor+Theme.h"
#import "UIFont+Theme.h"
#import "UIImage+Theme.h"

#endif
